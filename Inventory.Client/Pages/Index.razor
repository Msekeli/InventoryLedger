@page "/"
@inject NavigationManager Nav


<h2 class="mb-4">Dashboard</h2>

<div class="card p-4 shadow-sm mb-3">
    <h4>Welcome back, Derz 👋</h4>
    <p class="text-muted">Select a section from the navigation bar to get started.</p>
</div>

<div class="row">
    <div class="col-md-4 mb-3">
        <div class="card p-3 shadow-sm">
            <h5>Items</h5>
            <p>Manage inventory items.</p>
            <button class="btn btn-primary" @onclick="@(() => Nav.NavigateTo("/items"))">
                Go to Items
            </button>
        </div>
    </div>

    <div class="col-md-4 mb-3">
        <div class="card p-3 shadow-sm">
            <h5>Transactions</h5>
            <p>Record stock in and out.</p>
            <button class="btn btn-primary" @onclick="@(() => Nav.NavigateTo("/transactions"))">
                View Transactions
            </button>
        </div>
    </div>

    <div class="col-md-4 mb-3">
        <div class="card p-3 shadow-sm">
            <h5>Inventory Summary</h5>
            <p>Current stock levels overview.</p>
            <button class="btn btn-primary" @onclick="@(() => Nav.NavigateTo("/inventory/summary"))">
                View Summary
            </button>
        </div>
    </div>
</div>

@code {
   
}
